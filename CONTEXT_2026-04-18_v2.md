# SUS RAIO-X — Contexto Completo da Sessão
**Data:** 2026-04-18 | **Branch:** main | **Repo:** andremendessousa/susagil
**Versão:** v2 — substitui CONTEXT_2026-04-17.md e CONTEXT_2026-04-18.md

---

## 1. SISTEMA — O QUE É

**SUS Raio-X** — sistema GovTech para gestão de filas de exames de imagem e consultas
especializadas do SUS em **Montes Claros/MG**.

**Edital:** CPSI Co.NE nº 004/2026 — Prefeitura de Montes Claros / ENAP / Sudene / BID
**Submissão MVP:** 28/04/2026 | **Aceleração:** 18/mai → 10/jul/2026 | **Demoday:** 03–07/ago/2026
**Valores:** R$200k (CPSI) → R$1,6M (contrato subsequente)

**Problema resolvido:** vagas de exame canceladas em cima da hora ficavam ociosas. O sistema:
1. Convoca automaticamente o próximo paciente da fila (FIFO clínico) ao haver cancelamento
2. Monitora ocupação de equipamentos em tempo real
3. Comunica com pacientes via WhatsApp
4. Oferece assistente de IA conversacional para o gestor público

**Stack:** React 18 + Vite + Tailwind | Supabase (PostgreSQL + Realtime + Auth + RPC SECURITY DEFINER)

**Metas do edital CPSI 004/2026:**
| KPI | Baseline | Meta |
|---|---|---|
| Absenteísmo | ~35% | ≤ 15% |
| Tempo médio espera | > 120 dias | ≤ 120 dias |
| Aproveitamento de capacidade | ~65% | ≥ 85% |
| Satisfação usuário | ? | > 8/10 |

---

## 2. ARQUITETURA — VISÃO GERAL

```
frontend/src/
  pages/
    DashboardPage.jsx          ← KPIs + gráficos em tempo real
    AnaliseGerencialPage.jsx   ← análise aprofundada por tipo/período
    FilaPage.jsx               ← gestão da fila de espera
    MaquinasPage.jsx           ← equipamentos e capacidade
    NotificacoesPage.jsx       ← Kanban 3 boards (vagas → notificações → histórico)
    WhatsappPage.jsx           ← chat operador + painel paciente dark
    ConfiguracoesPage.jsx      ← configuração de metas KPI
    LoginPage.jsx
    AssistenteIAPage.jsx       ← NOVO: assistente conversacional Claude Sonnet
  lib/
    orquestracao.js            ← wrapper executar_reaproveitamento
    supabase.js
  hooks/
    useNotifications.js
    useAuth.js
    useKpiConfigs.js / useKpiConfigsMutation.js
    useDashboardMetrics.js     ← 9 RPCs paralelas, KPIs principais
    useDashboardCharts.js      ← 6 RPCs, dados para gráficos
    useQueue.js / useEquipment.js
  components/
    Layout.jsx                 ← menu lateral com item "Assistente IA" + badge "Novo"
    AgendarModal.jsx
    NovoEncaminhamentoModal.jsx
    SeletorEscopo.jsx
  contexts/
    EscopoContext.jsx
  constants/
    macrorregiao.js

supabase/migrations/
  202604120001_multifonte_kpis.sql
  202604150001_rpc_reaproveitamento.sql       (v1)
  202604150002_fix_rpc_reaproveitamento.sql   (v2)
  202604150003_fix_rpc_fifo_criteria.sql      (v3 ATIVA no Supabase)
  202604160001_fix_rpc_null_proc_fallback.sql (v4 rascunho — NÃO aplicada)
  202604170001_dedup_patients_seed.sql
  202604170002_reset_demo_seed_controlado.sql
```

---

## 3. BANCO DE DADOS — SCHEMA CRÍTICO

> **REGRA INVARIÁVEL:** Sempre confirmar schema atual com queries no Supabase SQL Editor
> antes de criar ou modificar qualquer RPC.

### 3.1 Tabelas principais

| Tabela | Campos críticos |
|---|---|
| `patients` | `id, cns, nome, telefone` — seed gerou duplicatas por nome (SQL de limpeza em 202604170001) |
| `queue_entries` | `patient_id, ubs_id, tipo_atendimento, nome_grupo_procedimento (nullable!), status_local, prioridade_codigo, data_solicitacao_sisreg, municipio_paciente, cor_risco` |
| `appointments` | **SEM `patient_id` direto** — acesso via `queue_entry_id → queue_entries.patient_id`. Campos: `equipment_id, scheduled_at, authorized_at, status (agendado/confirmado/cancelado/realizado), st_paciente_avisado (smallint NULLABLE — não é boolean!), st_falta_registrada, reaproveitado_de_id` |
| `notification_log` | `patient_id, appointment_id, tipo, canal, mensagem, resposta_paciente (confirmou/cancelou/null/'sem_notificacao'), enviado_at, respondido_at, entregue` |
| `equipment` | `ubs_id, nome, tipo_atendimento, turno, capacidade_dia` |
| `ubs` | `id, nome, tipo ('R'=reguladora), cnes_code, municipio` |

### 3.2 View
`v_dashboard_fila` — join de queue_entries + patients + ubs + appointments + equipment

### 3.3 Domínio geográfico e operacional
- **Municípios da macrorregião:** Montes Claros (sede polo), Claro dos Poções, Janaúba, Bocaiúva, Pirapora, Salinas
- **Unidades executantes:** Hospital Aroldo Tourinho, Hospital Universitário (HU), ImageMed (terceirizada), Ambulatório de Especialidades
- **Siglas:** US = Ultrassom | RX = Raio-X | TC = Tomografia Computadorizada | MRI = Ressonância Magnética

---

## 4. INVENTÁRIO COMPLETO DE RPCs

### 4.1 RPCs de KPI — usadas pelos dashboards (NÃO ALTERAR SEM PLANO)

| RPC | Parâmetros | Retorno | Usado em |
|---|---|---|---|
| `calcular_absenteismo` | `p_horizonte_dias, p_tipo_atendimento` | `{taxa_absenteismo, faltas, total_finalizados}` | useDashboardMetrics, AssistenteIA |
| `calcular_tempo_medio_espera` | `p_horizonte_dias, p_tipo_atendimento` | `{espera_atual_dias}` | useDashboardMetrics, AssistenteIA |
| `calcular_taxa_confirmacao_ativa` | `p_horizonte_dias, p_tipo_atendimento` | `{taxa_confirmacao}` | useDashboardMetrics, AssistenteIA |
| `calcular_taxa_reaproveitamento` | `p_horizonte_dias, p_tipo_atendimento, p_janela_horas` | `{taxa_reaproveitamento}` | useDashboardMetrics, AnaliseGerencialPage, AssistenteIA |
| `calcular_indice_satisfacao` | `p_horizonte_dias, p_tipo_atendimento` | `{nota_media}` | useDashboardMetrics ⚠️ fatal* |
| `calcular_demanda_reprimida` | `p_horizonte_dias, p_tipo_atendimento, p_dias_limite` | `{total_reprimida}` | useDashboardMetrics ⚠️ fatal* |
| `fn_ocupacao_passada` | `p_dias_atras, p_tipo_atendimento` | `[{equipamento_nome, unidade_nome, pct_ocupacao, capacidade_total, exames_realizados}]` | useDashboardMetrics, useDashboardCharts, useEquipment, AssistenteIA |
| `fn_ocupacao_futura` | `p_dias_a_frente, p_tipo_atendimento` | `[{capacidade_total, vagas_comprometidas}]` | useDashboardMetrics ⚠️ fatal* |

> ⚠️ **ALERTA IDENTIFICADO (Fase 4):** `calcular_indice_satisfacao`, `calcular_demanda_reprimida`
> e `fn_ocupacao_futura` estão no bloco `Promise.all` tratado como **fatal error** em
> `useDashboardMetrics.js`. Se qualquer uma falhar (ex: sem dados de satisfação no seed demo),
> **derruba o dashboard inteiro**. A corrigir na Fase 4 — mover para `Promise.allSettled` separado.

### 4.2 RPCs de gráfico — usadas pelos dashboards (NÃO ALTERAR SEM PLANO)

| RPC | Parâmetros | Retorno | Usado em |
|---|---|---|---|
| `get_tendencia_absenteismo` | `p_horizonte_dias, p_tipo_atendimento, p_media_movel_dias` | `[{dia, total, faltas, taxa, taxa_media_movel}]` | useDashboardCharts |
| `get_exames_por_local` | `p_horizonte_dias, p_tipo_atendimento` | `[{equipamento_nome, unidade_nome, realizados, faltas, taxa_absenteismo}]` | useDashboardCharts |
| `get_demanda_por_municipio` | `p_horizonte_dias, p_tipo_atendimento` | `[{municipio, uf, total_encaminhamentos, pct_do_total}]` | useDashboardCharts, AssistenteIA |
| `get_absenteismo_por_executante` | `p_horizonte_dias, p_tipo_atendimento` | `[{equipamento_nome, unidade_nome, taxa_absenteismo, total_agendado, total_faltas}]` | useDashboardCharts, AssistenteIA |
| `get_ubs_menor_espera` | `p_horizonte_dias, p_tipo_atendimento` | `[{ubs_nome, municipio, espera_media_dias, total_pacientes}]` | useDashboardCharts, AssistenteIA |

### 4.3 RPC de operação FIFO

| RPC | Parâmetros | Retorno | Versão |
|---|---|---|---|
| `executar_reaproveitamento` | `p_vaga_cancelada_id (uuid)` | `{nomeConvocado, erro}` | v3 ATIVA |

**Critérios FIFO v3 (em ordem):** mesma `ubs_id` → mesmo `tipo_atendimento` → mesmo
`nome_grupo_procedimento` → menor `prioridade_codigo` → menor `data_solicitacao_sisreg`

**Guarda de segurança:** se `nome_grupo_procedimento` for NULL no appointment cancelado,
encerra sem reaproveitamento.

**v4 (rascunho local, NÃO aplicada):** adiciona fallback UBS+tipo sem proc.
Tem PASSO 2C cross-UBS **rejeitado pelo CTO** (sem base clínica). Antes de aplicar: remover
o bloco PASSO 2C inteiro.

### 4.4 RPCs utilitárias

| RPC | Função |
|---|---|
| `derive_cor_risco(p_codigo smallint)` | Mapeia código 1–4 → enum cor_risco (vermelho/amarelo/verde/azul) |
| `map_sisreg_status_to_local(p_status_sisreg text)` | ~19 estados SISREG → 6 estados locais |
| `fn_susagil_sync_cor_risco` | Trigger que atualiza `cor_risco` automaticamente em `queue_entries` |

---

## 5. ASSISTENTE IA — ESTADO ATUAL (após Fase 1, 2026-04-17)

### 5.1 Arquitetura 3 camadas

```
Camada 1 — Interface React (AssistenteIAPage.jsx)
  Chat com histórico de mensagens, input livre, auto-scroll, loading indicator
  Histórico: state [messages], cada item: { id, role ('gestor'|'ia'), text, timestamp }

Camada 2 — Classificador de Intenção
  Claude API · max_tokens=100 · temperature=0
  Input: pergunta isolada → Output: {"intencao": "...", "parametros": {"dias":N, "unidade":"X"}}
  9 intents + fallback multi-turn

Camada 3 — Executor RPC + Narrador
  Executor: switch(intencao) → supabase.rpc(...) → JSON agregado (sem dados nominais)
  Narrador: Claude API · max_tokens=600–700 · temperature=0.3
  Input: pergunta + JSON de dados → Output: narrativa em PT-BR para gestor público
```

### 5.2 Configuração técnica

| Item | Valor |
|---|---|
| Modelo | `claude-sonnet-4-20250514` |
| URL API (dev) | `/api/anthropic/v1/messages` (proxy Vite) |
| URL API (prod/TODO) | Supabase Edge Function |
| Proxy Vite | `changeOrigin: true` + `proxy.on('proxyReq', removeHeader('origin'))` |
| API Key | `VITE_ANTHROPIC_API_KEY` em `.env.local` (não commitar) |

**Por que o proxy remove o header `origin`:** o Anthropic detecta requests de browser
pelo header `Origin`. Com `changeOrigin: true` o Vite troca apenas o `Host`, mas passa
adiante o `Origin`. A remoção explícita via `proxyReq.removeHeader('origin')` faz o
Anthropic tratar a request como server-to-server, eliminando o erro CORS.

### 5.3 Intents mapeados (v2 — pós Fase 1)

| Intent | RPC(s) executada(s) | Descrição |
|---|---|---|
| `ajuda` | nenhuma | Retorna `MENSAGEM_BOAS_VINDAS` localmente, zero tokens |
| `situacao_geral` | 5 RPCs em paralelo | KPIs: absenteísmo, espera, confirmação, reaproveitamento, ocupação |
| `equipamentos_ociosos` | `fn_ocupacao_passada` | Filtra `pct_ocupacao < 40%`, ordena ASC |
| `equipamentos_sobrecarregados` | `fn_ocupacao_passada` | Filtra `pct_ocupacao > 80%`, ordena DESC |
| `absenteismo_executante` | `get_absenteismo_por_executante` | Top 10 piores taxas por hospital/clínica |
| `detalhe_executante` ✨ | `get_absenteismo_por_executante` | Re-executa RPC + filtra por `parametros.unidade` (match parcial) |
| `espera_ubs` ✨ | `get_ubs_menor_espera` | Renomeado de `absenteismo_ubs` — dado real = tempo de espera, não falta |
| `demanda_municipal` | `get_demanda_por_municipio` | Top 15 municípios por volume de encaminhamentos |
| `recomendacoes` | 4 RPCs em paralelo | Cross-análise + top 3 executantes com mais faltas |
| `fora_de_escopo` | nenhuma (multi-turn) | Com histórico: Claude responde no contexto; sem histórico: mensagem padrão |

✨ = novo/alterado na Fase 1

### 5.4 SYSTEM_CLASSIFICADOR (resumo das melhorias da Fase 1)
- 9 intents com **exemplos coloquiais** por categoria (reduz classificações erradas)
- Instrução de extração de parâmetros: `{"dias": 7}` para períodos + `{"unidade": "nome"}` para `detalhe_executante`
- Nota explícita: "absenteísmo nas UBSs" → mapeia para `espera_ubs` (dado disponível = tempo de espera)
- Regras de combinação: `{"dias": 30, "unidade": "Aroldo Tourinho"}`

### 5.5 SYSTEM_NARRADOR (melhorias da Fase 1)
- **Glossário de domínio embutido:** municípios, siglas (US/RX/TC/MRI), metas do edital, unidades executantes
- **Regra anti-alucinação:** em follow-ups sem JSON de dados frescos, proibido inventar números — citar apenas dados já presentes no histórico visível ou perguntar se deve recarregar
- **Graceful degradation:** dados vazios/null → dizer honestamente + sugerir ampliar período
- Referência a `tipo_dado: 'tempo_de_espera_por_ubs_encaminhadora'` que o narrador usa para não confundir espera com absenteísmo

### 5.6 Segurança
- Nunca enviar dados nominais à API Anthropic (nome, CNS, telefone)
- Apenas métricas agregadas no JSON ao narrador
- TODO produção: mover para Edge Function (ver Fase 5)

---

## 6. PÁGINAS — ESTADO ATUAL

### DashboardPage.jsx
Usa `useDashboardMetrics` + `useDashboardCharts`. Exibe 8 KPI cards + 5 gráficos.

### AnaliseGerencialPage.jsx
Análise filtrada por tipo de atendimento e período.
Usa `calcular_taxa_reaproveitamento` diretamente (linha 137).
⚠️ **ALERTA:** sem tratamento diferenciado de `data === null` vs `data?.taxa_reaproveitamento === null`.
Pode exibir "—" em vez de "0%" quando RPC retorna objeto vazio. Corrigir na Fase 4.

### FilaPage.jsx
Usa `useQueue.js` — estado da fila em tempo real via Supabase Realtime.

### MaquinasPage.jsx
Usa `useEquipment.js` → `fn_ocupacao_passada`.

### NotificacoesPage.jsx — Kanban 3 boards
- **Board 1** — Vagas sem confirmação
  Query: `appointments.status='agendado'` + `.or('st_paciente_avisado.is.null,st_paciente_avisado.eq.0')` + `.gte('scheduled_at', now)`
- **Board 2** — Notificações enviadas (aguardando resposta)
- **Board 3** — Histórico (confirmados + cancelados + badge "Vaga Recuperada")
- Race condition B1→B2 resolvida via `notification_log.appointment_id` como truth source
- Optimistic UI: remove imediatamente ao notificar, rollback se INSERT falhar

### WhatsappPage.jsx — 3 colunas
- Coluna 1 (w-72): lista de contatos — merge `notification_log` + `appointments`
- Coluna 2 (flex-1): chat central, modo operador, **sem input**
- Coluna 3 (w-80): PainelPaciente — background `#1a1a2e`, header `#075e54`
- Sentinel `'sem_notificacao'`: pacientes agendados sem histórico WhatsApp recebem badge cinza "Ag. Notif."
- Double-check de cancelamento: state local `doubleCheck` no PainelPaciente

### ConfiguracoesPage.jsx
Usa `useKpiConfigs` + `useKpiConfigsMutation` — edição das metas dos KPIs cards.

---

## 7. PLANO DE EVOLUÇÃO — PRÓXIMAS FASES

---

### ✅ FASE 1 — Correções imediatas no AssistenteIA (CONCLUÍDA 2026-04-17)

1. ✅ Renomeado `absenteismo_ubs` → `espera_ubs` — alinha intent com dado real
2. ✅ Novo intent `detalhe_executante` — re-executa RPC + filtro por nome, elimina alucinação em follow-ups
3. ✅ SYSTEM_CLASSIFICADOR expandido — exemplos coloquiais por intent
4. ✅ SYSTEM_NARRADOR com glossário de domínio e regra anti-alucinação
5. ✅ Graceful degradation — arrays vazios retornam `{ aviso }` em vez de confundir o narrador
6. ✅ Build validado — 2272 módulos, zero erros, zero dashboards afetados

---

### 🔲 FASE 2 — 5 novas RPCs + intents

**Objetivo:** cobrir perguntas que hoje retornam `fora_de_escopo` ou dados incompletos.
**Arquivo a criar:** `supabase/migrations/202604180002_rpc_assistente_ia_v2.sql`

> ⚠️ Antes de criar: confirmar schema exato das tabelas com queries no Supabase SQL Editor.

#### 2.1 `get_fila_por_ubs(p_horizonte_dias, p_tipo_atendimento)`
**Responde:** "Qual UBS tem maior fila?", "Onde está a demanda represada?"
```sql
-- queue_entries com status_local IN ('aguardando','agendado') → agrupa por ubs
-- Retorno: {ubs_nome, municipio, total_aguardando, pct_do_total, espera_media_dias}
SELECT
  u.nome AS ubs_nome,
  u.municipio,
  COUNT(qe.id) AS total_aguardando,
  ROUND(COUNT(qe.id)*100.0 / NULLIF(SUM(COUNT(qe.id)) OVER (), 0), 1) AS pct_do_total,
  AVG(EXTRACT(EPOCH FROM (NOW()-qe.data_solicitacao_sisreg))/86400)::int AS espera_media_dias
FROM queue_entries qe
JOIN ubs u ON u.id = qe.ubs_id
WHERE qe.status_local IN ('aguardando','agendado')
  AND ($1 IS NULL OR qe.data_solicitacao_sisreg >= NOW() - ($1||' days')::interval)
GROUP BY u.id, u.nome, u.municipio
ORDER BY total_aguardando DESC
```
**Novo intent:** `fila_por_ubs`

#### 2.2 `get_fila_por_clinica(p_horizonte_dias, p_tipo_atendimento)`
**Responde:** "Qual clínica tem maior fila?", "Qual hospital está mais cheio na espera?"
```sql
-- queue_entries → appointments → equipment → ubs executante
-- Retorno: {equipamento_nome, unidade_nome, total_aguardando, capacidade_disponivel, pct_carga_fila}
```
**Novo intent:** `fila_por_clinica`

#### 2.3 `get_desempenho_por_ubs(p_horizonte_dias)`
**Responde:** "Qual o desempenho por UBS?", "Ranking das UBSs", "Qual UBS está melhor?"
```sql
-- Compõe score: (100 - absenteismo_pct)*0.4 + ((120-LEAST(espera,120))/120)*60
-- Retorno: {ubs_nome, municipio, absenteismo_pct, espera_media_dias, total_atendidos, score_composto}
```
**Novo intent:** `desempenho_ubs`

#### 2.4 `get_tipos_exame_solicitados(p_horizonte_dias, p_tipo_atendimento)`
**Responde:** "Quais exames são mais solicitados?", "Tipo de procedimento mais pedido?"
```sql
-- queue_entries.nome_grupo_procedimento (com fallback para tipo_atendimento)
-- Retorno: {tipo_exame, total_solicitacoes, pct_do_total, espera_media_dias}
SELECT
  COALESCE(qe.nome_grupo_procedimento, qe.tipo_atendimento, 'Não informado') AS tipo_exame,
  COUNT(*) AS total_solicitacoes,
  ROUND(COUNT(*)*100.0 / NULLIF(SUM(COUNT(*)) OVER(), 0), 1) AS pct_do_total,
  AVG(EXTRACT(EPOCH FROM (NOW()-qe.data_solicitacao_sisreg))/86400)::int AS espera_media_dias
FROM queue_entries qe
WHERE $1 IS NULL OR qe.data_solicitacao_sisreg >= NOW() - ($1||' days')::interval
GROUP BY tipo_exame
ORDER BY total_solicitacoes DESC LIMIT 15
```
**Novo intent:** `tipos_exame`

#### 2.5 `get_espera_por_municipio(p_horizonte_dias)`
**Responde:** "Qual o tempo de espera por município?", "Como estão pacientes de fora?"
Difere de `get_demanda_por_municipio` (conta encaminhamentos): este calcula espera real.
```sql
-- queue_entries.municipio_paciente com JOIN appointments para absenteísmo
-- Retorno: {municipio, total_pacientes, espera_media_dias, pct_absenteismo}
```
**Novo intent:** `espera_por_municipio`

#### 2.6 Candidatos extras (RPCs já existem nos dashboards, só criar intents)
| Intent a criar | RPC existente | Perguntas cobertas |
|---|---|---|
| `tendencia_absenteismo` | `get_tendencia_absenteismo` | "Tendência dos últimos dias", "Gráfico de faltas" |
| `exames_por_local` | `get_exames_por_local` | "Quais locais realizaram mais exames?" |

> Essas duas RPCs já existem — custo zero de SQL. Só adicionar intent + case no AssistenteIAPage.

---

### 🔲 FASE 3 — Enriquecimento de contexto Anthropic

#### 3.1 Prompt Caching (disponível em todos os planos Anthropic)
```javascript
// Economia: ~90% do custo + ~30% latência no SYSTEM_NARRADOR (~600 tokens)
// Mudança em chamarAnthropic():
//   system: string  →  system: [{ type:'text', text:'...', cache_control:{type:'ephemeral'} }]
//   Header adicional: 'anthropic-beta': 'prompt-caching-2024-07-31'
// Aplicar: APENAS no narrador. Classificador < 100 tokens — sem benefício.
```

#### 3.2 Atribuição de fonte temporal
```javascript
// Adicionar ao JSON do narrador:
const periodoFim    = new Date()
const periodoInicio = new Date(Date.now() - dias * 86_400_000)
// dadosRPC: { periodo_inicio: '17/04/2026', periodo_fim: '17/05/2026', ...dadosRPC }
// Instrução no SYSTEM_NARRADOR: "mencione sempre 'Dados de {periodo_inicio} a {periodo_fim}'"
```

#### 3.3 Seletor de período no header do chat
```jsx
// State: const [diasPadrao, setDiasPadrao] = useState(30)
// Dropdown no header: "7 dias / 30 dias / 60 dias / 90 dias"
// Substituir hardcode 30 por diasPadrao em executarQuery
```

---

### 🔲 FASE 4 — Qualidade, estabilidade e features

#### 4.1 Correção de RPCs fatais no useDashboardMetrics.js
```javascript
// BUG IDENTIFICADO: calcular_indice_satisfacao, calcular_demanda_reprimida, fn_ocupacao_futura
// estão num único Promise.all com anyError como fatal — derruba o dashboard inteiro.
// FIX: separar em Promise.allSettled([ calcular_indice_satisfacao, calcular_demanda_reprimida,
//        fn_ocupacao_futura ]) e exibir "—" nos cards sem travar os outros KPIs.
```

#### 4.2 Correção em AnaliseGerencialPage.jsx
```javascript
// BUG: calcular_taxa_reaproveitamento (linha 137) sem tratar data === null vs
//      data?.taxa_reaproveitamento === null separadamente.
// FIX: const valor = data?.taxa_reaproveitamento ?? 0
//      Exibir "0%" em vez de "—" quando taxa é genuinamente zero.
```

#### 4.3 Feedback de resposta (👍 / 👎)
```sql
-- Nova migration: 202604180003_assistente_ia_feedback.sql
CREATE TABLE assistente_ia_feedback (
  id          uuid        PRIMARY KEY DEFAULT gen_random_uuid(),
  session_id  text        NOT NULL,   -- gerado no client: Math.random().toString(36)
  message_id  text        NOT NULL,   -- id da MensagemBubble
  pergunta    text,
  intencao    text,
  rating      smallint    NOT NULL CHECK (rating IN (1, -1)),
  created_at  timestamptz NOT NULL DEFAULT now()
);
```
```jsx
// Botões 👍/👎 em MensagemBubble role='ia'
// supabase.from('assistente_ia_feedback').insert({ session_id, message_id, rating })
```

#### 4.4 Persistência de histórico no localStorage
```javascript
// Salvar: useEffect(() => localStorage.setItem('susraiox_chat', JSON.stringify(messages)), [messages])
// Restaurar: useState(() => { const s = localStorage.getItem('susraiox_chat')
//              return s ? JSON.parse(s).slice(-30) : [WELCOME_MSG] })
```

---

### 🔲 FASE 5 — Segurança e produção

#### 5.1 Mover chamadas Anthropic para Supabase Edge Function
```
// RISCO ATUAL: VITE_ANTHROPIC_API_KEY fica no bundle JS (visível em DevTools)
// SOLUÇÃO: supabase/functions/assistente-ia/index.ts
//   Frontend: supabase.functions.invoke('assistente-ia', { body: { pergunta, historico } })
//   Edge Function: executa as 2 chamadas Anthropic com key segura via env var
//   Benefício extra: pode chamar RPCs Supabase internamente sem expor URL ao modelo
```

#### 5.2 Rate limiting
```
// Na Edge Function: max 20 chamadas/minuto por user_id
// Resposta 429 com mensagem PT-BR: "Muitas perguntas em seguida — aguarde 1 minuto"
```

---

## 8. MAPA COMPLETO: PERGUNTAS → INTENTS → STATUS

| Pergunta do gestor | Intent | RPC | Status |
|---|---|---|---|
| "Como pode me ajudar?" / "O que você faz?" | `ajuda` | — | ✅ Funciona |
| "Como está a fila?" / "Me dá um resumo" / "Situação hoje" | `situacao_geral` | 5 RPCs | ✅ Funciona |
| "Tem máquina parada?" / "Equipamentos ociosos" / "Aparelhos livres" | `equipamentos_ociosos` | `fn_ocupacao_passada` | ✅ Funciona |
| "O que está mais cheio?" / "Gargalo de capacidade" / "Aparelho lotado" | `equipamentos_sobrecarregados` | `fn_ocupacao_passada` | ✅ Funciona |
| "Qual hospital tem mais falta?" / "Absenteísmo por executante" | `absenteismo_executante` | `get_absenteismo_por_executante` | ✅ Funciona |
| "Detalhe o Aroldo Tourinho" / "Como está a ImageMed?" / "Foque no HU" | `detalhe_executante` | `get_absenteismo_por_executante` | ✅ Novo Fase 1 |
| "Espera nas UBSs?" / "Onde a fila é maior?" / "Absenteísmo nas UBSs?" | `espera_ubs` | `get_ubs_menor_espera` | ✅ Renomeado Fase 1 |
| "De onde vêm os pacientes?" / "Municípios da macrorregião" | `demanda_municipal` | `get_demanda_por_municipio` | ✅ Funciona |
| "O que eu faço?" / "Quais as prioridades?" / "Como melhorar?" | `recomendacoes` | 4 RPCs | ✅ Funciona |
| "Qual UBS tem maior fila?" / "Demanda represada por UBS" | `fila_por_ubs` | `get_fila_por_ubs` (criar) | 🔲 Fase 2 |
| "Qual clínica tem maior fila?" / "Hospital com mais gente esperando" | `fila_por_clinica` | `get_fila_por_clinica` (criar) | 🔲 Fase 2 |
| "Ranking das UBSs" / "Qual UBS está melhor?" / "Desempenho por unidade" | `desempenho_ubs` | `get_desempenho_por_ubs` (criar) | 🔲 Fase 2 |
| "Quais exames são mais solicitados?" / "Procedimento mais pedido" | `tipos_exame` | `get_tipos_exame_solicitados` (criar) | 🔲 Fase 2 |
| "Tempo de espera por município?" / "Quanto espera quem vem de fora?" | `espera_por_municipio` | `get_espera_por_municipio` (criar) | 🔲 Fase 2 |
| "Tendência dos últimos dias" / "Gráfico de faltas" | `tendencia_absenteismo` | `get_tendencia_absenteismo` (existe!) | 🔲 Fase 2 — só intent |
| "Quais locais realizaram mais exames?" | `exames_por_local` | `get_exames_por_local` (existe!) | 🔲 Fase 2 — só intent |

---

## 9. PENDÊNCIAS HERDADAS

### Urgente
- [ ] **Dedup patients** (`202604170001_dedup_patients_seed.sql`): rodar SELECT diagnóstico, depois descomentar UPDATE+DELETE e executar no SQL Editor
- [ ] **Commits pendentes** — AssistenteIA + Fase 1 nunca commitados. Ver seção 11.
- [ ] **RPC v4** (`202604160001`): remover PASSO 2C (cross-UBS rejeitado) antes de aplicar

### Médio prazo
- [ ] **Template persistence WhatsappPage:** templates desaparecem ao navegar — inserir em `notification_log` ao disparar
- [ ] **st_paciente_avisado NULL:** validar `DEFAULT 0` no schema de produção

### Baixa prioridade
- [ ] Chunk size warning no build (> 500KB) — considerar lazy loading por página

---

## 10. DECISÕES ARQUITETURAIS FIXADAS

| Decisão | Resolução |
|---|---|
| Cross-UBS fallback no RPC FIFO | **REJEITADO** — sem base clínica para determinar UBS "mais próxima" |
| Double-check de cancelamento | No PainelPaciente (state local `doubleCheck`) — não no chat central |
| Linguagem no PainelPaciente | Linguagem paciente ("Sua vaga foi para o próximo da fila") |
| Linguagem no chat central | Linguagem operador ("✅ Vaga reaproveitada — X convocado") |
| Footer do chat WhatsApp | Sem input, sem botões — apenas label modo Operador |
| `ubs.tipo` para reguladoras | `'R'` (bug antigo: `'S','A'`) |
| Intent `absenteismo_ubs` | **Renomeado para `espera_ubs`** — dado real é tempo de espera por UBS de origem |
| Contexto Anthropic | No código (versionado git) + Prompt Caching — não Anthropic Projects |
| API Anthropic no frontend | Dev: proxy Vite com `removeHeader('origin')` | Prod: Edge Function (Fase 5) |

---

## 11. COMANDOS ESSENCIAIS

```bash
# Build de validação
cd /workspaces/susagil/frontend && npx vite build

# Dev server (proxy Anthropic ativo em /api/anthropic)
cd /workspaces/susagil/frontend && npm run dev

# Commit das features AssistenteIA + Fase 1 (PENDENTE)
cd /workspaces/susagil
git add frontend/src/pages/AssistenteIAPage.jsx \
        frontend/src/App.jsx \
        frontend/src/components/Layout.jsx \
        frontend/vite.config.js
git commit -m "feat(assistente-ia): assistente conversacional com Claude Sonnet

- Arquitetura 3 camadas: classificador → executor RPC → narrador
- Proxy Vite para Anthropic API (CORS resolvido via removeHeader origin)
- 10 intents mapeados para RPCs reais do Supabase
- Fase 1: intent espera_ubs renomeado, novo detalhe_executante
- Exemplos coloquiais no classificador, glossário de domínio no narrador
- Regra anti-alucinação para follow-ups sem dados frescos"
git push origin main
```

---

## 12. FLUXO DEMO COMPLETO (banca 28/04/2026)

### Módulo FIFO + WhatsApp (~3 min)
1. **Notificações** → Board 1: vagas sem confirmação → clicar "Notificar" → move para Board 2
2. **WhatsApp** → paciente aparece com badge "Pendente" → selecionar → 3 colunas
3. Painel direito: "1 ✅ SIM" → painel vira verde, "Agendamento confirmado!"
4. Outro paciente: "2 ❌ NÃO" → double-check → "CONFIRMAR DESISTÊNCIA" → RPC → "✅ Vaga reaproveitada — [Nome]"

### Módulo AssistenteIA (~3 min)
1. Abrir **Assistente IA** no menu lateral
2. "Como está a fila hoje?" → KPIs reais com análise
3. "Qual equipamento está mais parado?" → lista ociosos em ordem
4. "Qual hospital tem mais falta?" → ranking por taxa de absenteísmo
5. "Detalhe o Hospital Aroldo Tourinho" → re-fetch + análise focada (sem alucinação)
6. "O que eu faço pra melhorar?" → recomendações cruzadas

### Módulo Dashboard (~2 min)
1. KPIs: absenteísmo, espera, ocupação × metas do edital
2. Gráfico de tendência + demanda por município + ocupação por executante

---

*Arquivo gerado em 2026-04-18 (v2). Substitui CONTEXT_2026-04-17.md e CONTEXT_2026-04-18.md.*

---

## 13. FASE 2 — ZONA 6 DO DASHBOARD (concluída 2026-04-18)

### Arquivos criados/modificados
| Arquivo | O que mudou |
|---------|-------------|
| `supabase/migrations/202604180001_rpc_fila_desempenho.sql` | 5 novas RPCs para análise de fila e desempenho |
| `frontend/src/hooks/useDashboardChartsV2.js` | Novo hook — chama as 5 RPCs em `Promise.allSettled` |
| `frontend/src/pages/DashboardPage.jsx` | ZONA 6 com 5 novos gráficos (Cards E–I) + import `Award` + `useDashboardChartsV2` |

### 5 Novas RPCs (migration 0008)

| RPC | Pergunta | Params | Retorno |
|-----|----------|--------|---------|
| `get_fila_por_ubs` | "Qual UBS tem maior fila?" | `p_horizonte_dias, p_tipo_atendimento` | `[{ubs_nome, municipio, total_aguardando, pct_do_total, espera_media_dias}]` |
| `get_fila_por_clinica` | "Qual clínica está sobrecarregada?" | `p_horizonte_dias, p_tipo_atendimento` | `[{equipamento_nome, unidade_nome, municipio, vagas_comprometidas, capacidade_periodo, pct_carga_fila}]` |
| `get_desempenho_por_ubs` | "Ranking das UBSs" | `p_horizonte_dias` | `[{ubs_nome, municipio, absenteismo_pct, espera_media_dias, total_atendidos, score_composto}]` |
| `get_tipos_exame_solicitados` | "Quais exames mais solicitados?" | `p_horizonte_dias, p_tipo_atendimento` | `[{tipo_exame, total_solicitacoes, pct_do_total, espera_media_dias}]` |
| `get_espera_por_municipio` | "Espera por município?" | `p_horizonte_dias` | `[{municipio, total_pacientes, espera_media_dias, pct_absenteismo}]` |

### Correções de schema aplicadas na migration
- `queue_entries.tipo_atendimento` é `USER-DEFINED` (enum) — todas as comparações com `text` usam `::text` explícito
- `COALESCE(nome_grupo_procedimento, tipo_atendimento::text, 'Não informado')` — cast necessário no SELECT e GROUP BY da RPC 4
- Padrão validado: **RPCs 1, 2, 3, 4, 5 aplicadas com sucesso** no Supabase

### Score composto (get_desempenho_por_ubs)
```
score = (100 - absenteismo_pct) × 0,5 + ((120 - min(espera, 120)) / 120) × 50
```
- Verde ≥ 80 · Âmbar ≥ 60 · Vermelho < 60

### Gráficos da ZONA 6 no Dashboard
| Card | Tipo | Dado |
|------|------|------|
| E — Volume de fila por UBS | BarChart horizontal | `fila_por_ubs.total_aguardando` |
| F — Carga de agenda por equipamento | BarChart horizontal + cores por threshold | `fila_por_clinica.pct_carga_fila` |
| G — Ranking de desempenho por UBS | BarChart horizontal + cores (verde/âmbar/vermelho) | `desempenho_ubs.score_composto` |
| H — Procedimentos mais solicitados | BarChart vertical | `tipos_exame.total_solicitacoes` |
| I — Espera e absenteísmo por município | BarChart duplo (azul + vermelho) | `espera_municipio.espera_media_dias + pct_absenteismo` |

Todos os cards usam `ChartSkeleton` durante loading e "Nenhum dado no período selecionado" quando empty — comportamento identical aos gráficos existentes (Zonas 2–3).

*Atualizado em 2026-04-18 — Fase 2 concluída.*

---

## 14. Sessão 2026-04-19 — Fix get_exames_por_local + Qualidade de código

### Problema diagnosticado
A intent `exames_por_local` no `AssistenteIA` retornava "não consegui identificar dados" mesmo com 286 registros `status='realizado'` no banco.

**Root cause (confirmado via `pg_get_functiondef`):**
A função `get_exames_por_local` existia em 2 overloads quebrados no Supabase:

| Overload | Problema |
|----------|---------|
| v1 `(integer)` | Usava `a.nome_unidade_executante` — coluna inexistente em `appointments` |
| v2 `(integer, text)` | Retornava `{nome, total}` — contrato incompatível com todos os callers |

O JS lia `e.equipamento_nome` → `undefined` → `Number(undefined) = NaN` → `NaN > 0 = false` → filter zerava todas as linhas → narrador recebia array vazio.

**Diagnóstico-chave:**
```sql
-- Confirma existência de dados:
SELECT status, count(*) FROM appointments GROUP BY status ORDER BY count(*) DESC;
-- → realizado=286, faltou=135

-- Expõe o contrato real da função:
SELECT pg_get_functiondef(oid) FROM pg_proc WHERE proname = 'get_exames_por_local';
-- → revelou retorno {nome, total} em vez de {equipamento_nome, unidade_nome, realizados, ...}
```

**Lição PostgreSQL:** `CREATE OR REPLACE FUNCTION` não pode alterar a lista de colunas de `RETURNS TABLE` → **ERROR 42P13**. É obrigatório `DROP FUNCTION` explícito antes de recriar.

### Migration 0009 aplicada
`supabase/migrations/202604190001_fix_rpc_exames_por_local.sql`

- DROP ambos overloads (v1 coluna inexistente + v2 contrato errado)
- CREATE com contrato canônico: `{equipamento_nome, unidade_nome, realizados, total_agendado, faltas, taxa_absenteismo}`
- Padrão LEFT JOIN: todos os equipamentos `status='ativo'` retornados, mesmo com `realizados=0`
- `taxa_absenteismo` alinhada com `calcular_absenteismo()`: denominador = `realizados + faltou` (não `total_agendado`)
- Bloco DO $$ valida: exatamente 1 overload + `get_exames_por_local(90, NULL)` retorna linhas

### Arquivos modificados nesta sessão

| Arquivo | Mudança |
|---------|---------|
| `frontend/src/pages/AssistenteIAPage.jsx` | Removido fallback `nota_fonte`; `case 'exames_por_local'` revertido para chamada limpa com campos canônicos; removida linha `nota_fonte` do `SYSTEM_NARRADOR` |
| `frontend/src/hooks/useDashboardMetrics.js` | `anyError` agora só checa 5 RPCs core (`absenteismo`, `espera`, `confirmacao`, `reaproveitamento`, `ocupacaoPassada`); `satisfacao`, `demandaReprimida`, `ocupacao_futura` retornam `null` gracefully |
| `frontend/src/pages/AnaliseGerencialPage.jsx` | Tooltip usa `total_agendado` (não `total_finalizados`); Realtime subscription em `queue_entries` → incrementa `filaRt` para refresh no donut de prioridade |
| `frontend/src/pages/MaquinasPage.jsx` | Badge `'Sobrecarregado'` → `'Alta utilização'` para `pct >= 85` |
| `supabase/migrations/202604190001_fix_rpc_exames_por_local.sql` | **NOVA** — Fix da função (ver acima) |

### Teste validado pelo usuário
```
Usuário: "ranking de produção"
Resposta: "Nos últimos 30 dias, o equipamento com maior volume de exames
           realizados é o RX-01 da UBS Aroldo Tourinho, com 48 exames..."
           "...A clínica ImageMed apresenta taxa de absenteísmo de 12,5%..."
```

Build validado: **2273 módulos, zero erros**.

---

## 15. Estado atual e próximos passos

### Fase 3 — CONCLUÍDA ✅
Todas as 7 intents do AssistenteIA funcionando com dados reais:
`absenteismo_geral`, `fila_por_ubs`, `fila_por_clinica`, `desempenho_ubs`, `tipos_exame`, `espera_por_municipio`, `tendencia_absenteismo`, `exames_por_local`

Funcionalidades de UX ativas: localStorage (chave `susraiox_chat`, 30 msgs), seletor de período (7/30/60/90 dias), botão limpar histórico, Prompt Caching Anthropic no narrador.

### Próximos — Fase 4

#### P0 — Segurança (obrigatório antes do MVP 28/04)
- Mover chamadas Anthropic para **Supabase Edge Function** — a `VITE_ANTHROPIC_API_KEY` está exposta no bundle JS (visível em DevTools → Network)
- Rate limiting: 20 req/min por `user_id` no Edge Function

#### P1 — Feedback do usuário no AssistenteIA
- Migration: `202604180003_assistente_ia_feedback.sql` (tabela `ia_feedback` com `msg_id`, `user_id`, `voto`, `timestamp`)
- UI: botões 👍/👎 no componente `MensagemBubble` — POST ao Supabase após clique

#### P2 — AnaliseGerencialPage
- Corrigir `data?.taxa_reaproveitamento ?? 0` (pode estar `undefined` causando NaN no gauge)

#### P3 — Seed de pacientes dedupados
- Diagnóstico: `SELECT cpf, count(*) FROM patients GROUP BY cpf HAVING count(*) > 1;`
- Se duplicatas encontradas: aplicar `202604170001_dedup_patients_seed.sql`

#### P4 — RPC v4 (cross-UBS)
- `202604160001` — remover PASSO 2C antes de aplicar (requer análise de impacto)

### Padrões invariantes do projeto

| Padrão | Descrição |
|--------|-----------|
| **LEFT JOIN para métricas** | `equipment LEFT JOIN appointments` — todos os equipamentos ativos aparecem mesmo sem dados no período |
| **taxa_absenteismo** | Denominador = `realizados + faltou` (total_finalizados), não `total_agendado` |
| **Contrato de RPC** | Sempre `RETURNS TABLE (...)` explícito; nunca `RETURNS SETOF record` |
| **DROP antes de CREATE** | Quando mudar `RETURNS TABLE` columns: DROP + CREATE (não OR REPLACE) — PG ERROR 42P13 |
| **anyError no dashboard** | Apenas 5 RPCs core são fatais; `satisfacao`/`demandaReprimida`/`ocupacao_futura` são graceful |
| **Chat storage** | `susraiox_chat` no localStorage, limite 30 mensagens, timestamps como ISO string |
| **Prompt Caching** | Somente no narrador (`useCache: true`), não no classificador |
| **Anti-alucinação** | `SYSTEM_NARRADOR`: "NUNCA invente nomes de pessoas, unidades ou equipamentos que não apareçam nos dados" |

*Atualizado em 2026-04-19 — Sessão: fix get_exames_por_local + commit Fase 3.*

---

## 16. Sessão 2026-04-20 — Reseed completo de recuperação (CPSI 004/2026)

### Contexto do problema

O seed `202604200001` (anterior) destruiu os dados demo de Montes Claros ao executar
`DELETE FROM queue_entries WHERE data_source = 'seed_demo'` de forma global, apagando
também entradas que não pertenciam ao escopo do seed. A tabela ficou com dados insuficientes
para sustentar a narrativa do edital.

A sessão consistiu em: (1) corrigir e executar 3 novos arquivos de seed scoped e recuperar
todos os dados demo; (2) corrigir 4 páginas frontend e `macrorregiao.js` para alinhamento
com o novo escopo de 3 níveis.

---

### Arquivos de seed criados (3 migrations)

| Arquivo | Propósito |
|---------|-----------|
| `supabase/migrations/202604210001_seed_cleanup_infra.sql` | BLOCO 0: cleanup seguro scoped + BLOCO A: 22 UBSs + 15 equipment |
| `supabase/migrations/202604210002_seed_pacientes_historico.sql` | BLOCO B: 52 pacientes + BLOCO C: histórico 30 dias (appointments passados) |
| `supabase/migrations/202604210003_seed_fila_pipeline.sql` | BLOCO D: fila aguardando (~30 entradas) + BLOCO E: pipeline 3 boards + BLOCO F: verificação final |

---

### Escopo Geográfico e de UBSs

#### Infraestrutura criada (BLOCO A)

**UBSs Solicitantes — tipo='S' (14 total)**

| Grupo | UBS | CNES (seed) | Município |
|-------|-----|-------------|-----------|
| Independência | Independência II | 9999201 | Montes Claros |
| Maracanã | Maracanã II, III, IV | 9999202–04 | Montes Claros |
| Major Prates | Major Prates I, II, III, IV | 9999205–08 | Montes Claros |
| Outros MC | São José e Lourdes III | 9999209 | Montes Claros |
| Outros MC | Cintra I, II | 9999210–11 | Montes Claros |
| Macrorregião | UBS Bocaiúva | 9999221 | Bocaiúva |
| Macrorregião | UBS Pirapora | 9999222 | Pirapora |
| Macrorregião | UBS Janaúba | 9999223 | Janaúba |

**UBSs Executantes — tipo='R' (8 total)**

| UBS | CNES (seed) | Observação narrativa |
|-----|-------------|----------------------|
| Hospital Aroldo Tourinho | 9999301 | Alto absenteísmo — justifica o sistema |
| Hospital das Clínicas Dr. Mário Ribeiro | 9999302 | Referência de qualidade |
| Hospital Universitário | 9999303 | Infraestrutura reserva (sem appointments no período) |
| Santa Casa de Montes Claros | 9999304 | Média |
| Fundação Dilson Godinho | 9999305 | Média |
| HU Clemente de Faria — Ortopedia | 9999306 | Regional Independência |
| OrthoMed Clínica Especializada | 9999307 | Parceiro privado, baixíssimo absenteísmo |
| ImageMed Clinica de Imagem | 9999308 | Terceirizada |

**Equipment (15 itens):** vinculados às 8 UBSs executantes via lookup por nome (UPDATE + INSERT IF NOT EXISTS).

---

### Dados Narrativos por Bloco

#### BLOCO B — 52 pacientes (CPSI 004/2026)

| Município de residência | Qtd | Escopo narrativo |
|-------------------------|-----|-----------------|
| Montes Claros | 47 | MC + Regional Independência |
| Bocaiúva | 2 | Macrorregião |
| Pirapora | 2 | Macrorregião |
| Janaúba | 1 | Macrorregião |

- 22 pacientes com `ubs_origem = Independência II` (escopo Regional drill-down)
- CNS prefixo `800001` + `data_source='seed_demo'` → cleanup scoped seguro

#### BLOCO C — Histórico 30 dias (56 appointments passados)

Absenteísmo por hospital — alinhado com narrativa do edital (meta ≤ 15%):

| Hospital | Realizados | Faltas | Absenteísmo | Narrativa |
|----------|-----------|--------|-------------|-----------|
| Hospital Aroldo Tourinho | 8 | 5 | **38,5%** | Alto — principal justificativa do sistema |
| Santa Casa de Montes Claros | 4 | 2 | 33,3% | Acima da média |
| HU Clemente de Faria | 5 | 2 | 28,6% | Regional Independência |
| ImageMed Clinica de Imagem | 5 | 2 | 28,6% | Terceirizada |
| Fundação Dilson Godinho | 3 | 1 | 25,0% | Média |
| Hospital das Clínicas Dr. Mário Ribeiro | 7 | 2 | **22,2%** | Referência qualidade |
| OrthoMed Clínica Especializada | 9 | 1 | **10,0%** | Parceiro privado exemplar |

**Absenteísmo global:** ~27% → bem acima da meta ≤ 15%, criando urgência narrativa para o sistema.
**Hospital Universitário:** sem appointments no período — reserva técnica, não aparece no ranking.

#### BLOCO D — Fila aguardando (~30 entradas)

Distribuição por prioridade:

| Prioridade | Cor | Qtd | Casos notáveis |
|-----------|-----|-----|----------------|
| 1 — Urgência | Vermelho | 6 | Geraldo Nunes (65d, gonalgia aguda), Roberto Carlos (55d, fratura) |
| 2 — Médio | Amarelo | 12 | Valdir (78d, joelho), Luíza (60d, coluna) |
| 3 — Menor | Verde | 8 | Sebastião (70d, ombro), Conceição (58d) |
| 4 — Rotina | Azul | 4 | Sebastião (130d! — gancho narrativo prio-4 esquecido) |

**Ponto narrativo chave:** Sebastião Rodrigues (prio 4, 130 dias de espera), Irene Batista (prio 4, 110 dias) — casos de rotina perpetuamente preteridos pela triagem clínica, ilustrando o problema de equidade que o FIFO endereça.

FIFO Independência II (20 entradas visíveis no escopo Regional), ordenado corretamente por
`prioridade_codigo ASC, data_solicitacao_sisreg ASC`.

#### BLOCO E — Pipeline 3 boards (19 appointments futuros)

| Board | Critério | Qtd | Pacientes notáveis |
|-------|----------|-----|--------------------|
| Board 3 — Histórico | `resposta_paciente IS NOT NULL` | 3 | Conceição ✅, Sílvio ✅, Edilson ❌ |
| Board 2 — Aguard. confirmação | `st_paciente_avisado=1, resposta_paciente IS NULL` | 4 | Terezinha, Luíza, Débora, Fábio |
| Board 1 — Pendente notificação | `st_paciente_avisado IS NULL` | 12 | Irene, Adilson, Marlene + 9 MC |

**Reaproveitamentos FIFO (3 badges):**
- **Geraldo Nunes** (prio 1) → convocado após cancelamento de Edilson → `reaproveitado_de_id` aponta para appointment de Edilson
- **Fábio Alexandre** (Board 2, OrthoMed) → também `reaproveitado_de_id` set
- **Osvaldo Cunha** (Board 1, Das Clínicas) → também `reaproveitado_de_id` set

Estes 3 badges demonstram a funcionalidade central do FIFO na UI.

#### BLOCO F — Totais finais validados

| Tabela | Total seed_demo |
|--------|-----------------|
| `patients` | 52 |
| `queue_entries` | 105 (56 histórico + 30 fila + 19 pipeline) |
| `appointments` | 75 (56 histórico + 19 pipeline) |
| `notification_log` | 403 (~7 por appointment no histórico) |

---

### Erros runtime encontrados e resoluções

Três erros em sequência durante a execução do BLOCO 0 e BLOCO A (arquivo 1):

#### Erro 1: `23503 FK appointments_equipment_id_fkey`
**Causa:** BLOCO 0 tentou DELETE em `equipment` com appointments ainda referenciando esses
equipamentos via `equipment_id` direto (não apenas via `queue_entry_id`).

**Fix:** Adicionado passo 2b antes de deletar `equipment`:
```sql
DELETE FROM appointments
WHERE equipment_id IN (
  SELECT eq.id FROM equipment eq
  JOIN ubs u ON u.id = eq.ubs_id
  WHERE u.cnes_code LIKE '9999%'
);
```

#### Erro 2: `P0001 Nenhum equipment ativo encontrado`
**Causa:** Após BLOCO 0 deletar todo o equipment seed, `SELECT FROM equipment WHERE status='ativo'`
retornava vazio. O bloco ENUM usava isso para copiar os tipos.

**Fix:** Mudado para `SELECT ... FROM equipment LIMIT 1` (qualquer registro, ativo ou não).
Adicionado fallback via `pg_enum` para quando a tabela está vazia.

#### Erro 3: `3F000 schema "equipment" does not exist`
**Causa:** `::equipment.tipo_recurso%TYPE` é sintaxe válida apenas em seções `DECLARE` do
PL/pgSQL — não em expressões SQL dentro do bloco.

**Fix:** Variável intermediária `v_enum_text text` + coerção implícita do PL/pgSQL:
```sql
-- Em DECLARE:
v_enum_text text;

-- No corpo:
SELECT e.enumlabel INTO v_enum_text
FROM pg_enum e JOIN pg_attribute a ON a.atttypid = e.enumtypid
WHERE a.attrelid = 'equipment'::regclass AND a.attname = 'tipo_recurso'
ORDER BY e.enumsortorder LIMIT 1;
v_tr := v_enum_text;  -- PL/pgSQL converte text → ENUM implicitamente em atribuição
```

#### Erro 4: `42601 INSERT has more expressions than target columns`
**Causa:** 3 INSERTs em `queue_entries` (Nilda, Quirino, Bernardo) tinham `'Montes Claros','MG'`
duplicado nos VALUES — 14 valores para 12 colunas.

**Fix:** Remoção do par duplicado nos 3 INSERTs. Mesmo padrão já corrigido em sessões
anteriores (arquivo 2 e 3 — `municipio_paciente, uf_paciente` adicionados por edição e
acidentalmente duplicados na geração).

---

### Lições aprendidas (2026-04-20)

#### PostgreSQL / PL/pgSQL

| Lição | Regra prática |
|-------|---------------|
| `%TYPE` em DECLARE vs em SQL | `v_tr equipment.tipo_recurso%TYPE` é válido **apenas** na seção DECLARE. Dentro do corpo do DO $$, nunca usar `::equipment.tipo_recurso%TYPE` em casts SQL — usará sintaxe regular de schema, não de tipo. |
| Coerção text → ENUM em PL/pgSQL | Atribuição `v_tr := 'algum_valor'` com `v_tr` declarado como `%TYPE` funciona automaticamente se o literal for um label válido do ENUM. Não precisa de cast explícito. |
| Leitura de ENUMs sem dados na tabela | Usar `pg_enum JOIN pg_attribute` com `attrelid = 'tabela'::regclass AND attname = 'coluna'` — retorna os labels mesmo com a tabela vazia. |
| Ordem de deleção com FKs | Ao fazer cleanup, sempre mapear todas as FKs transitivas. `appointments.equipment_id` é uma FK direta que o ON DELETE RESTRICT bloqueia mesmo quando o caminho óbvio é via `queue_entry_id`. |
| DELETE scoped vs global | **Nunca usar** `DELETE FROM tabela WHERE data_source = 'x'` como único critério quando há FKs. Sempre deletar na ordem inversa das FKs e usar critérios de escopo (CNS LIKE, CNES LIKE). |
| Contagem de colunas em INSERT | Em INSERTs longos com muitos campos, contar explicitamente colunas vs valores. Erros de duplicata de par `municipio, uf` são difíceis de ver visualmente. |

#### Estratégia de seed

| Lição | Regra prática |
|-------|---------------|
| Seed destrutivo é perigoso | Nunca usar DELETE global por `data_source` sem transação scoped que reverta se algo falhar. |
| Separar em 3 arquivos sequenciais | Infraestrutura → Pacientes/Histórico → Fila/Pipeline é a ordem correta de dependências. Permite re-executar cada bloco isoladamente ao debugar. |
| Fallback de ENUMs em seed | Todo DO $$ que cria dados com colunas ENUM deve ter fallback hardcoded (`IF v_ta IS NULL THEN v_ta := 'consulta'`) para rodar após cleanup total. |
| Validação inline com RAISE NOTICE | Adicionar `RAISE NOTICE '✅ ...'` ao final de cada sub-bloco facilita localizar onde um erro ocorreu sem re-executar tudo. |
| Verificação com SELECT ao final | Sempre fechar cada arquivo com queries de verificação em comentário — ajuda a confirmar os dados sem re-abrir o SQL Editor manualmente. |

#### Coerência de dados com RPCs e Frontend

| Aspecto | O que verificar | Resultado neste seed |
|---------|-----------------|----------------------|
| `get_demanda_por_municipio` | Usa `queue_entries.municipio_paciente` | ✅ Preenchido em todas as 105 entradas |
| `get_fila_por_ubs` | Usa `ubs_id + status_local IN ('aguardando','agendado')` | ✅ 30 entradas aguardando + 19 agendadas |
| `get_desempenho_por_ubs` | Usa `appointments.status IN ('realizado','faltou')` | ✅ 56 appointments históricos corretamente distribuídos |
| Boards NotificacoesPage | `st_paciente_avisado IS NULL` (Board 1), `resposta_paciente IS NULL` (Board 2), `resposta_paciente IS NOT NULL` (Board 3) | ✅ 12/4/3 |
| Reaproveitamentos | `reaproveitado_de_id IS NOT NULL` | ✅ 3 badges (Geraldo, Fábio, Osvaldo) |
| FIFO ordenação | `prioridade_codigo ASC, data_solicitacao_sisreg ASC` | ✅ 20 entradas Independência II |

---

### Estado do git (pendente commit)

Arquivos novos/modificados ainda não commitados:

```bash
cd /workspaces/susagil
git add supabase/migrations/202604210001_seed_cleanup_infra.sql
git add supabase/migrations/202604210002_seed_pacientes_historico.sql
git add supabase/migrations/202604210003_seed_fila_pipeline.sql
git add frontend/src/constants/macrorregiao.js
git add frontend/src/pages/WhatsappPage.jsx
git add frontend/src/pages/FilaPage.jsx
git add frontend/src/pages/DashboardPage.jsx
git add frontend/src/pages/AnaliseGerencialPage.jsx
git commit -m "feat: reseed completo ortopedia + fix escopos frontend (CPSI 004/2026)

- Migration 202604210001: cleanup scoped (CNES 9999%) + 22 UBSs + 15 equipment
- Migration 202604210002: 52 pacientes (47 MC + 5 macrorregião) + histórico 56 appointments
- Migration 202604210003: 30 entradas fila aguardando + pipeline 3 boards + 3 badges reaproveitamento
- Absenteísmo narrativo: Aroldo 38.5% | Clínicas 22.2% | OrthoMed 10.0% (meta <15% a atingir)
- Fix: municipio_paciente em todas as queue_entries para RPCs get_demanda_por_municipio
- Fix: 3 UBSs macrorregionais (Bocaiúva/Pirapora/Janaúba) para escopo Macrorregião
- Fix: escopos frontend (3 níveis: Macrorregião ⊃ MC ⊃ Independência)"
git push origin main
```

---

### Próximos passos (dia 21/04)

1. **Commit** (ver acima) — pendente
2. **Verificações funcionais** em tela: abrir cada página e confirmar que os dados aparecem corretamente nos gráficos, fila e boards
3. **Pequenas correções identificadas** pelo usuário (detalhar na próxima sessão)
4. **Fase 4 P0** — Mover Anthropic API para Supabase Edge Function antes do MVP 28/04

*Atualizado em 2026-04-20 — Sessão: reseed completo de recuperação ortopedia CPSI 004/2026.*

---

## 17. Sessão 2026-04-24 — Seed Final Demo + Fix de Bugs Visuais (Pré-Gravação)

### Contexto

Esta é a **sessão pré-gravação definitiva** (banca 28/04/2026). O banco estava funcionalmente
correto (RPCs e frontend validados), mas com volume de dados insuficiente para sustentar os
KPIs da narrativa do edital: absenteísmo global ~27%, mas ocupação passada ~9%, reaproveitamento
300% (denominador = 1), e painel de UBS aguardando vazio no escopo Regional Independência.

Três bugs visuais foram corrigidos no frontend + cinco arquivos de seed foram planejados e criados.
**Após execução, este banco fica congelado para gravação.**

---

### Bugs visuais corrigidos

| Bug | Causa | Arquivo corrigido | Fix |
|-----|-------|------------------|-----|
| Flicker no seletor Municipal | `escopoMunicipal` recalculado a cada render gerando novo array `[]` | `frontend/src/pages/DashboardPage.jsx` | Envolvido em `useMemo` (4 variáveis de escopo) |
| Reaproveitamento 300% | Apenas 1 cancelado no banco como denominador; 3 reaproveitados | seed (corrigido via arquivo 5) | +11 cancelamentos → denominador = 12 → 3/12 = 25% |
| Painel "Menor espera" vazio (Regional) | `get_ubs_menor_espera` ignorava UBSs sem appointments realizados | `supabase/migrations/202604230001_fix_ubs_menor_espera.sql` | RPC reescrita para incluir UBSs com só aguardando |
| `useRef` não importado | Import incompleto em `AnaliseGerencialPage.jsx` | `frontend/src/pages/AnaliseGerencialPage.jsx` | Adicionados `useRef, useCallback` nos imports |

---

### Arquivos de seed criados (5 migrations)

| Arquivo | Conteúdo | Volume |
|---------|----------|--------|
| `supabase/migrations/202604240001_rollback_seed_final.sql` | Rollback: DELETE WHERE `data_source='seed_final_demo'` na ordem correta de FKs | — |
| `supabase/migrations/202604240002_seed_historico_aroldo.sql` | +78 appointments — Hospital Aroldo Tourinho (3 equipamentos) | +78 |
| `supabase/migrations/202604240003_seed_historico_clemente.sql` | +90 appointments — HU Clemente de Faria Ortopedia (2 equipamentos) | +90 |
| `supabase/migrations/202604240004_seed_historico_outros.sql` | +83 appointments — ImageMed, Das Clínicas, Santa Casa, OrthoMed, Dilson | +83 |
| `supabase/migrations/202604240005_seed_futuros_espera.sql` | +50 futuros + 11 cancelados + UPDATE `data_solicitacao_sisreg` + V1-V10 | +61 |

**Total novo:** 312 appointments (251 finalizados + 50 futuros + 11 cancelados)

#### Estratégia por arquivo

**Arquivo 2 — Aroldo Tourinho (narrativo: alto absenteísmo)**
- Equipamentos: `RX-01 — Aroldo Tourinho`, `RX-02 — Aroldo Tourinho`, `US-01 — Aroldo Tourinho`
- Absenteísmo: `v_i % 7 < 3` = **42,9% faltou** → pior do ranking (justifica urgência do sistema)
- Intervalo: 9h20m → 78 × 9h20m ≈ 30 dias → distribui uniformemente
- `data_solicitacao_sisreg` = `scheduled_at - (60 + i%81) dias` → espera histórica ~100 dias

**Arquivo 3 — HU Clemente de Faria (narrativo: Regional Independência)**
- Equipamentos: `Ortopedia — Consultório Manhã`, `Traumatologia — Consultório Tarde`
- Absenteísmo: `v_i % 10 < 3` = **30% faltou**
- Intervalo: 8h exatos → **3 por dia** → curva de tendência suave no gráfico regional
- UBS reguladora: ~60% Independência II (array com repetição intencional)
- `tipo_atendimento = 'consulta'` — ortopédico, não exame

**Arquivo 4 — 5 hospitais restantes (blocos A–E)**

| Hospital | Novos | Absenteísmo alvo | Fórmula |
|----------|-------|-----------------|---------|
| ImageMed | +28 | ~11% | `v_i % 9 = 0` |
| Das Clínicas | +22 | ~20% | `v_i % 5 = 0` |
| Santa Casa | +18 | ~33% | `v_i % 3 = 0` |
| OrthoMed | +8 | ~12% | `v_i % 8 = 0` |
| Dilson Godinho | +7 | ~29% | `v_i % 7 < 2` |

**Arquivo 5 — Futuros, cancelados, espera**
- 50 agendamentos futuros (6h–7d a partir de NOW): ocupam ~60% dos 105 slots (15 equip × 7d)
- 11 cancelamentos passados: corrige reaproveitamento de 300% para **25%** (3/12)
- `UPDATE queue_entries SET data_solicitacao_sisreg`: polo macrorregional (Bocaiúva=130d, Pirapora=145d, Janaúba=150d); MC recente ajustado para ≥ 50d
- Queries V1-V10 de verificação ao final

---

### Identificador de rollback

**Todos os rows novos têm `data_source = 'seed_final_demo'`.**

Se qualquer KPI sair errado após execução:
```sql
-- 1. Executar 202604240001_rollback_seed_final.sql
-- Isso apaga notification_log → appointments → queue_entries nessa ordem.
-- O seed base (data_source = 'seed_demo') é preservado integralmente.
```

---

### KPIs esperados após execução completa

| KPI | Estado atual (seed_demo) | Estado esperado (+ seed_final_demo) | Alvo da narrativa |
|-----|--------------------------|-------------------------------------|-------------------|
| Absenteísmo global | ~27% | **~30%** | 28–32% (acima da meta ≤15%) |
| Ocupação passada 30d | ~9% | **~48%** | 40–55% |
| Reaproveitamento | 300% | **~25%** | 15–30% |
| Ocupação futura 7d | ~17% | **~60%** | 55–70% |
| Espera histórica (avg realizados) | — | **~100 dias** | 70–130 dias |
| Espera atual (aguardando polo) | <30d | **130–150 dias** | >120d (polo) |

**Absenteísmo por hospital (ranking esperado):**

| Ranking | Hospital | Absenteísmo esperado | Narrativa |
|---------|----------|---------------------|-----------|
| 1º (pior) | Hospital Aroldo Tourinho | ~43% | Justificativa central |
| 2º | Santa Casa de Montes Claros | ~33% | Acima da média |
| 3º | HU Clemente de Faria | ~30% | Regional Independência |
| 4º | Fundação Dilson Godinho | ~28% | Média |
| 5º | ImageMed | ~14% | Terceirizada — ok |
| 6º | Hospital das Clínicas | ~20% | Referência — ok |
| 7º (melhor) | OrthoMed | ~11% | Parceiro exemplar |

---

### Ordem de execução no Supabase SQL Editor

> **CRÍTICO:** Executar exatamente esta ordem. Cada arquivo deve ser executado inteiro
> (selecionar tudo → Run). Verificar mensagens RAISE NOTICE após cada um.

```
PASSO 0 (se não executado ainda):
  202604230001_fix_ubs_menor_espera.sql    ← reescreve get_ubs_menor_espera

PASSO 1:
  202604240002_seed_historico_aroldo.sql   ← +78 apts Aroldo Tourinho

PASSO 2:
  202604240003_seed_historico_clemente.sql ← +90 apts HU Clemente

PASSO 3:
  202604240004_seed_historico_outros.sql   ← +83 apts (5 hospitais)

PASSO 4:
  202604240005_seed_futuros_espera.sql     ← +50 futuros +11 cancelados +UPDATE espera

PASSO 5 (validação):
  Executar as queries V1-V10 constantes no final do arquivo 5
  → Conferir cada KPI antes de gravar
```

**Se qualquer V1-V10 falhar:**
```
ROLLBACK:
  202604240001_rollback_seed_final.sql   ← apaga tudo com data_source='seed_final_demo'
  → Diagnosticar → corrigir → re-executar do PASSO 1
```

**Dependências entre arquivos:**
- 002, 003, 004 são independentes entre si (diferentes hospitais) — podem ser reexecutados individualmente via rollback parcial
- 005 depende do estado criado pelos três anteriores (cancela apts existentes + ajusta `data_solicitacao_sisreg` de todos os aguardando)

---

### Lições aprendidas (2026-04-24)

#### PostgreSQL — UUIDs e lookups em seeds

| Lição | Regra prática |
|-------|---------------|
| **Nunca hardcodar UUIDs de `gen_random_uuid()`** | IDs gerados por `gen_random_uuid()` em migrations são diferentes a cada execução se a migration for re-aplicada. UUIDs obtidos de queries diagnósticas no banco atual ficam desatualizados após rollback+reseed. Use sempre `SELECT id FROM equipment WHERE nome = '...'`. |
| **Nomes de equipment usam em-dash (—)** | O padrão no banco é `'RX-01 — Aroldo Tourinho'` (U+2014), não `'RX-01 - Aroldo Tourinho'` (hífen simples). Verificar na migration de criação antes de escrever lookup. |
| **`EXTRACT(EPOCH FROM uuid_col)` não existe** | Para pseudo-aleatório baseado em UUID, usar `abs(hashtext(id::text)) % N`. |
| **`LIMIT` não funciona com `ARRAY_AGG`** | `SELECT ARRAY_AGG(id) ... LIMIT 11` aplica o LIMIT antes do ARRAY_AGG, não depois — resulta em array com até 11 elementos MAS o comportamento é dependente do plano. Usar subquery: `SELECT ARRAY_AGG(id) FROM (SELECT id ... LIMIT 11) sub`. |

#### Estratégia de seed para demo

| Lição | Regra prática |
|-------|---------------|
| **Separar em arquivos por hospital/tema** | Evita atingir limites de tamanho do SQL Editor e permite re-executar apenas o hospital quebrado. Cada arquivo deve ser 100–250 linhas, autocontido. |
| **`data_source` como identificador de rollback** | Adicionar `data_source = 'seed_final_demo'` em todos os INSERTs é a única garantia de rollback cirúrgico sem afetar dados base. |
| **Lookup de enums com fallback** | O padrão `SELECT tipo_atendimento INTO v_ta FROM queue_entries WHERE tipo_atendimento::text = 'exame' LIMIT 1; IF v_ta IS NULL THEN SELECT ... LIMIT 1; END IF;` evita crash em banco recém-limpo. |
| **Absenteísmo por fórmula modular** | `v_i % N < K` é previsível, auditável e controlado. Antes de usar, calcular `K/N` e verificar se está no range alvo. Ex: `v_i % 7 < 3` = 42,9%; `v_i % 10 < 3` = 30,0%. |
| **Intervalo de tempo = volume ÷ período** | Para distribuir N appointments em M dias: intervalo = `(M * 24) / N` horas. Verificar antes de escrever o INTERVAL para evitar todos os apts no mesmo horário ou além do período. |
| **Validar com V1-V10 antes de gravar** | Nunca gravar vídeo sem executar as queries de verificação. KPIs podem parecer corretos mas estar calculados com denominador errado. |

#### Depuração de KPIs inconsistentes

| Sintoma | Causa mais provável | Como diagnosticar |
|---------|--------------------|--------------------|
| Reaproveitamento > 100% | Denominador (cancelados) = 0 ou muito baixo | `SELECT COUNT(*) FROM appointments WHERE status='cancelado'` |
| Ocupação passada < 20% | Poucos `realized_at` preenchidos OU `realized_at` fora do range da RPC | `SELECT COUNT(*) FROM appointments WHERE realized_at IS NOT NULL AND realized_at >= NOW()-'30 days'::interval` |
| Absenteísmo = 0% | `st_falta_registrada = 0` em todos os faltou OU RPC usa coluna errada | Conferir denominador da RPC: deve ser `realizados + faltou`, não `agendados` |
| Painel de espera vazio | RPC filtra `status_local IN ('realizado')` mas UBSs só têm 'aguardando' | Ver `get_ubs_menor_espera` — checar se exige realized_at para incluir UBS |

---

### Estado do git após esta sessão

Novos arquivos criados esta sessão (ainda não commitados junto com os da sessão 20/04):

```bash
cd /workspaces/susagil
git add supabase/migrations/202604230001_fix_ubs_menor_espera.sql
git add supabase/migrations/202604240001_rollback_seed_final.sql
git add supabase/migrations/202604240002_seed_historico_aroldo.sql
git add supabase/migrations/202604240003_seed_historico_clemente.sql
git add supabase/migrations/202604240004_seed_historico_outros.sql
git add supabase/migrations/202604240005_seed_futuros_espera.sql
git add frontend/src/pages/DashboardPage.jsx
git add frontend/src/pages/AnaliseGerencialPage.jsx
git commit -m "feat: seed final demo + fix bugs visuais pré-gravação (24/04/2026)

- 5 migrations de seed (202604240002-005): +312 appointments com data_source=seed_final_demo
- Rollback cirúrgico: 202604240001 deleta seed_final_demo preservando seed_demo
- Fix reaproveitamento 300% → 25% (+11 cancelamentos no arquivo 005)
- Fix ocupação passada ~9% → ~48% (+251 finalizados distribuídos por hospital)
- Fix get_ubs_menor_espera: inclui UBSs com apenas aguardando (202604230001)
- Fix flicker Dashboard Municipal: escopoMunicipal/escopoMC/escopoInd/escopoMacro em useMemo
- Fix import useRef/useCallback em AnaliseGerencialPage.jsx
- KPIs esperados pós-seed: absenteísmo 30%, ocupação 48%, reaprov 25%, futura 60%"
git push origin main
```

---

### Checklist pré-gravação (28/04/2026)

```
[ ] Executar 202604230001 no SQL Editor (fix get_ubs_menor_espera)
[ ] Executar 202604240002 no SQL Editor
[ ] Executar 202604240003 no SQL Editor
[ ] Executar 202604240004 no SQL Editor
[ ] Executar 202604240005 no SQL Editor
[ ] Rodar V1-V10 (fim do arquivo 005) — confirmar todos os KPIs
[ ] Abrir Dashboard → conferir absenteísmo ~30%, ocupação ~48%, reap ~25%
[ ] Abrir FilaPage → testar escopo Independência → painel "menor espera" aparece
[ ] Testar fluxo FIFO + WhatsApp completo (boards + reaproveitamento)
[ ] Testar AssistenteIA: "situação geral" → dados reais aparecem
[ ] Fazer commit (ver bloco acima)
[ ] Confirmar npm run build sem erros antes de gravar
```

*Atualizado em 2026-04-24 — Sessão: seed final demo + fix bugs visuais pré-gravação.*
